{
  "name": "AnimeDesktop",
  "description": "https://github.com/zhmushan/AnimeDesktop",
  "version": "0.1.1",
  "license": "GPL-3.0",
  "author": "木杉",
  "main": "dist/main.js",
  "scripts": {
    "start": "tsc && electron ./dist/main.js",
    "pack": "rimraf dist uwp && tsc && electron-packager . AnimeDesktop --platform=win32 --arch=x64 --out=. --icon=favicon.ico --asar"
  },
  "devDependencies": {
    "electron": "^8.5.1",
    "electron-packager": "^14.2.1",
    "rimraf": "^3.0.2",
    "typescript": "^3.8.3"
  }
}
